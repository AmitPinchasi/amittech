בהרצאה הקודמת הבנו את הרעיון של לולאת הסוכן. עכשיו נלכלך את הידיים. נפתח את Claude Code בתוך מאגר קוד אמיתי, ננהל איתו שיחה, נבקש ממנו לקרוא ולהסביר קוד, נבקש עריכה, נאשר או נדחה את השינוי שהוא מציע, ונבין את מחזור החיים של סשן. כל מה שנעשה כאן הוא הבסיס שכל שאר הפרק נשען עליו, אז שווה להריץ את הצעדים במקביל לקריאה על מאגר קטן משלכם.

נניח שהתקנתם כבר את Claude Code והתחברתם לחשבון (זה קורה בהרצה הראשונה, כשהכלי פותח לכם דפדפן להתחברות). אם לא, אפשר להתקין בפקודה אחת ולהריץ `claude` בפעם הראשונה כדי להתחבר. מכאן והלאה נניח שהכל מוכן.

---

## פתיחת סשן - starting claude

הצעד הראשון תמיד זהה: נכנסים לתיקיית הפרויקט ומריצים את הפקודה `claude`.

```bash
cd ~/projects/my-app
claude
```

ברגע שהסוכן עולה, תראו שורת הזנה שמעליה מופיעים מספר הגרסה, המודל הנוכחי (למשל Sonnet 5 או Opus 4.8), ותיקיית העבודה. עצם ההרצה בתוך התיקייה נותנת ל-Claude גישה לכל מה שהוא צריך: הקבצים בתיקייה ובתת-התיקיות, מצב ה-git (הענף הנוכחי, שינויים לא מקומטים, היסטוריית קומיטים אחרונה), וקובץ ה-CLAUDE.md אם קיים. אתם לא מוסיפים הקשר ידנית - הסוכן קורא מה שהוא צריך תוך כדי עבודה.

כמה פקודות הפעלה שימושיות מהטרמינל:

| פקודה | מה היא עושה |
| --- | --- |
| `claude` | פותח סשן אינטראקטיבי |
| `claude "task"` | מריץ משימה חד-פעמית |
| `claude -p "query"` | מריץ שאילתה בודדת ויוצא (מצב הדפסה) |
| `claude -c` | ממשיך את השיחה האחרונה בתיקייה |
| `claude -r` | מאפשר לבחור שיחה קודמת לחידוש |

בתוך הסשן, אפשר תמיד להקליד `/help` כדי לראות את הפקודות הזמינות, ולהקליד `/` כדי לראות את כל הפקודות והמיומנויות. שתי פקודות בסיסיות שכדאי לזכור מיד: `/clear` שמנקה את היסטוריית השיחה ומתחיל מחדש, ו-`/exit` (או Ctrl+D) שיוצא מהסשן.

---

## מודל השיחה - the conversation model

Claude Code הוא שיחתי במהותו. אתם לא צריכים לנסח פקודות מושלמות. מתחילים במה שרוצים, ואם התוצאה לא מדויקת - מכוונים. זו לא מערכת שבה טעות בניסוח שורפת את הניסיון; זו שיחה מתגלגלת.

הכלל המנחה: מתארים מטרה בשפה טבעית, לא נותנים רצף פקודות. במקום "פתח את הקובץ, גלול לשורה 40, שנה את X", אומרים "יש באג שבו טופס ריק מתקבל - תתקן אותו". הסוכן ימצא את הקוד לבד.

בזמן שהסוכן עובד, אתם עדיין חלק מהלולאה. שתי דרכים לכוון אותו תוך כדי:

- **לחיצה על Esc** עוצרת את Claude מיד. הכלי שרץ מבוטל, והסוכן ממתין להנחיה הבאה שלכם. העבודה שכבר נעשתה נשמרת.
- **הקלדת תיקון ולחיצה על Enter** שולחת הודעה בלי לעצור את הכלי הרץ. Claude יקרא אותה ברגע שהפעולה הנוכחית תסתיים, ויתאים את עצמו לפני הצעד הבא.

היכולת הזאת - להפריע ולכוון בלי להתחיל מהתחלה - היא מה שהופך את העבודה לשיחה אמיתית ולא לסדרת ניסיונות מנותקים.

---

## לבקש לקרוא ולהסביר קוד - explain

הצעד הראשון בכל מאגר חדש הוא להבין אותו. במקום לצלול ידנית לקבצים, שואלים את הסוכן. הנה שיחה טיפוסית לפתיחת מאגר:

```text
> מה הפרויקט הזה עושה?
```

Claude יסרוק את הקבצים ויחזיר תקציר. אפשר לחדד:

```text
> אילו טכנולוגיות הפרויקט משתמש בהן?
> איפה נקודת הכניסה הראשית?
> תסביר את מבנה התיקיות
```

מאחורי הקלעים, בכל שאלה כזאת הסוכן מריץ את הלולאה: הוא משתמש ב-Glob וב-Grep כדי למצוא קבצים רלוונטיים, ב-Read כדי לקרוא אותם, ואז מסכם. אתם רואים את הכלים רצים על המסך בזמן אמת. הנה איך זה עשוי להיראות עבור שאלה על נקודת הכניסה:

```text
> איפה נקודת הכניסה הראשית ומה קורה שם?

  Glob    **/main.py, **/app.py, **/index.js
  Read    src/app.py
  Read    src/config.py

נקודת הכניסה היא src/app.py. היא טוענת את ההגדרות מ-config.py,
מאתחלת את שרת ה-Flask, ורושמת שלושה ראוטים תחת /api. השרת
עולה על פורט 5000 כברירת מחדל.
```

שימו לב שלא אמרנו לו איפה לחפש. הוא הסיק בעצמו אילו שמות קבצים סבירים לנקודת כניסה, חיפש אותם, קרא את הרלוונטיים, וסיכם. זו הלולאה מהרצאה 2.1 בפעולה על משימה אמיתית.

---

## לבקש עריכה - asking for an edit

עכשיו נעבור מהבנה לשינוי. נבקש מהסוכן משהו קטן ומוגדר. הכלל: ככל שהבקשה ספציפית יותר, כך פחות סבב תיקונים. הנה דוגמה מוחשית של בקשת עריכה:

```text
> תוסיף פונקציה בשם validate_email לקובץ src/utils.py.
  היא מקבלת מחרוזת ומחזירה True אם זו כתובת מייל תקינה.
  מקרי בדיקה: 'user@example.com' -> True, 'invalid' -> False.
  תריץ את הבדיקות אחרי.
```

הבקשה הזאת טובה כי היא נותנת לסוכן משהו לאמת מולו - מקרי הבדיקה. Claude יאתר את הקובץ הנכון, יכתוב את הפונקציה, ולפי הבקשה גם יריץ בדיקות כדי לוודא שהיא עובדת. אבל לפני שהוא נוגע בקובץ, הוא יראה לכם מה בכוונתו לשנות. זה מוביל אותנו לחלק הקריטי הבא.

---

## לאשר או לדחות שינוי - accepting and rejecting diffs

Claude Code **תמיד מבקש אישור לפני שהוא משנה קובץ**. כשהוא רוצה לערוך, הוא מציג לכם diff - הפרש - שמראה בדיוק אילו שורות נוספות (בירוק) ואילו נמחקות (באדום), ומחכה לתשובתכם. הנה איך זה נראה:

```text
  Edit src/utils.py

  + def validate_email(value: str) -> bool:
  +     import re
  +     pattern = r"^[^@\s]+@[^@\s]+\.[^@\s]+$"
  +     return re.match(pattern, value) is not None

  Do you want to make this edit to utils.py?
  > 1. Yes
    2. Yes, and allow edits during this session
    3. No, tell Claude what to do differently (Esc)
```

יש כאן שלוש אפשרויות, וכדאי להבין את ההבדל ביניהן:

- **Yes** - מאשר את השינוי הבודד הזה בלבד. בפעם הבאה שהסוכן ירצה לערוך, הוא ישאל שוב.
- **Yes, and allow edits during this session** - מאשר, ומעביר את הסשן למצב שבו עריכות קבצים לא ידרשו אישור נוסף (מצב שנלמד לעומק בהרצאה 2.3).
- **No** - דוחה את השינוי. חשוב: כשדוחים, מסבירים ל-Claude מה לעשות אחרת. הדחייה היא לא סוף הדרך אלא כיוון מחדש: "לא, אל תשתמש בביטוי רגולרי, תשתמש בספריית email-validator".

הדחייה היא כלי עבודה, לא כישלון. אם ה-diff לא מוצא חן בעיניכם - הגישה שגויה, הסגנון לא מתאים, או שהוא נוגע בקובץ הלא נכון - דוחים ומסבירים. הסוכן יתקן ויציג diff חדש. כך שומרים על שליטה: שום שינוי לא נכנס לקוד שלכם בלי שראיתם ואישרתם אותו.

---

## עבודה עם git - git integration

אחת התכונות הנוחות היא ש-Claude Code הופך פעולות git לשיחתיות. הוא רואה את מצב ה-git שלכם ויכול לפעול עליו:

```text
> אילו קבצים שיניתי?
> תעשה commit לשינויים עם הודעה תיאורית
> תפתח ענף חדש בשם feature/email-validation
```

גם כאן, כל פעולה שמשנה משהו (כמו commit) עוברת דרך אותו מנגנון אישור. הסוכן ינסח הודעת commit, יראה לכם אותה, ורק אחרי אישור יבצע. זה חוסך הרבה הקלדה של פקודות git ידניות, אבל שומר אתכם בשליטה.

---

## להיות ספציפי ולתת מול מה לאמת - be specific

מהרצאה 2.1 אנחנו יודעים שהסוכן חזק ככל שיש לו מול מה לאמת. בפועל זה מתורגם לשני הרגלים שמשפרים דרמטית את התוצאות מהריצה הראשונה.

הראשון: **היו ספציפיים מראש**. ככל שהבקשה הפותחת מדויקת יותר, כך פחות תיקונים. השוו:

```text
גרסה מעורפלת:
> תקן את הבאג

גרסה ספציפית:
> תהליך התשלום שבור עבור משתמשים עם כרטיס שפג תוקף.
  תבדוק את src/payments/, במיוחד את רענון האסימון.
  תכתוב קודם בדיקה שנכשלת, ואז תתקן.
```

הגרסה הספציפית מפנה לקובץ, מציינת מגבלה, ומגדירה תהליך. היא לרוב מצליחה מהניסיון הראשון, בעוד המעורפלת תדרוש כמה סבבי כיוון.

השני: **תנו לסוכן משהו לבדוק מולו**. כשאפשר, כללו מקרי בדיקה, פלט צפוי, או צילום מסך של ממשק רצוי. הסוכן יריץ את הבדיקה ויתקן את עצמו עד שהיא עוברת. שני ההרגלים האלה, ספציפיות ומשהו לאמת מולו, הם ההבדל בין סוכן שקולע בפעם הראשונה לבין סוכן שדורש הרבה כיוון ידני.

---

## מחזור החיים של סשן - session lifecycle

בואו נסכם את הזרימה של סשן טיפוסי מקצה לקצה. זהו הדפוס שתחזרו עליו כל יום:

```text
1. cd אל תיקיית הפרויקט, ואז claude   -> הסשן עולה
2. שאלות הבנה                          -> "מה זה עושה?", "איפה X?"
3. בקשת שינוי מוגדרת                    -> "תוסיף את הפונקציה Y"
4. סקירת ה-diff ואישור/דחייה            -> Yes או No עם הסבר
5. אימות                                -> "תריץ את הבדיקות"
6. commit                              -> "תעשה commit עם הודעה"
7. /exit או Ctrl+D                      -> יציאה
```

נקודה חשובה על סשנים: **כל סשן מתחיל עם חלון הקשר נקי**. אם תצאו ותפתחו סשן חדש, Claude לא זוכר את השיחה הקודמת. יש לזה שני פתרונות. הראשון הוא לחדש שיחה קיימת עם `claude -c` (המשך אחרון) או `claude -r` (בחירת שיחה). השני, החשוב יותר לטווח ארוך, הוא לכתוב הנחיות קבועות בקובץ CLAUDE.md כדי שהן ייטענו בכל סשן מחדש. את זה נלמד בהרצאה 2.5.

עוד רשת ביטחון ששווה להכיר כבר עכשיו: **כל עריכת קובץ ניתנת לביטול**. לפני ששינה קובץ, Claude שומר תמונת מצב שלו. אם משהו השתבש, אפשר ללחוץ Esc פעמיים כדי לפתוח תפריט חזרה אחורה (rewind) ולשחזר מצב קודם, או פשוט לבקש מ-Claude לבטל. נקודות השחזור האלה מקומיות לסשן ונפרדות מ-git, והן מכסות רק שינויי קבצים - לא פעולות עם השלכות חיצוניות כמו קריאות רשת או פריסות, ולכן על אלה הסוכן תמיד מבקש אישור.

---

## דוגמה מלאה מקצה לקצה - a full session

נחבר הכל לסשן אחד רציף על מאגר קטן של סקריפט פייתון:

```text
$ cd ~/projects/todo-cli
$ claude

> מה הפרויקט הזה עושה?
  כלי שורת פקודה לניהול רשימת מטלות, שומר אותן בקובץ JSON.
  נקודת הכניסה היא todo.py.

> יש באג: כשמריצים 'add' בלי טקסט, התוכנית קורסת. תתקן.
  Read  todo.py
  מצאתי: הפונקציה cmd_add ניגשת ל-args[1] בלי לבדוק שהוא קיים.

  Edit todo.py
  + if len(args) < 2:
  +     print("שגיאה: חסר טקסט למטלה")
  +     return
  Do you want to make this edit? > 1. Yes

> תריץ את זה בלי ארגומנט כדי לוודא
  Bash  python todo.py add
  שגיאה: חסר טקסט למטלה   (התוכנית לא קרסה. תוקן.)

> תעשה commit עם הודעה מתאימה
  git commit -m "תיקון קריסה בהוספת מטלה ריקה"  > 1. Yes
```

בסשן הזה עברנו את כל המחזור: הבנה, זיהוי באג, עריכה מאושרת, אימות בהרצה, ו-commit - הכל בשיחה טבעית, בלי שכתבנו בעצמנו אף שורת קוד או אף פקודת git.

---

## סיכום

- מתחילים סשן ב-`cd` לתיקיית הפרויקט ואז `claude`. הסוכן מקבל אוטומטית גישה לקבצים, ל-git ול-CLAUDE.md.
- העבודה **שיחתית**: מתארים מטרה בשפה טבעית, ומכוונים אם צריך. Esc עוצר את הסוכן; הקלדת תיקון מכוונת אותו בלי לעצור.
- שואלים את הסוכן להבין את הקוד ("מה זה עושה?", "איפה נקודת הכניסה?"), והוא מפעיל Glob, Grep ו-Read בעצמו.
- לבקשת עריכה **ספציפית** עם מקרי בדיקה מקבלים תוצאה טובה יותר, כי יש לסוכן מול מה לאמת.
- Claude **תמיד מציג diff ומבקש אישור** לפני עריכה. אפשר לאשר (Yes), לאשר לכל הסשן, או לדחות (No) ולהסביר מה לעשות אחרת.
- פעולות git הופכות שיחתיות, ועוברות דרך אותו מנגנון אישור.
- כל סשן מתחיל עם **הקשר נקי**; מחדשים עם `claude -c`/`-r`, ומשמרים הנחיות קבועות ב-CLAUDE.md.
- כל עריכת קובץ **ניתנת לביטול** דרך נקודות שחזור (Esc פעמיים), כרשת ביטחון נפרדת מ-git.
