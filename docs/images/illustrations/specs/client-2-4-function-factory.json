{
  "title": "פונקציה שמחזירה פונקציה: createMultiplier יוצרת את double שזוכרת את factor",
  "layout": { "type": "stack" },
  "components": [
    { "id": "factory", "type": "code", "lines": [
        "function createMultiplier(factor) {",
        "    return (number) => number * factor;",
        "}"
      ] },
    { "id": "call", "type": "code", "label": "const double = createMultiplier(2)" },
    { "id": "made", "type": "code", "label": "double = (number) => number * 2" },
    { "id": "use", "type": "code", "label": "double(5)",
      "values": ["double(5)", "5 * 2", "10"] },
    { "id": "note", "type": "text", "label": "הפונקציה שנוצרה זוכרת את הערך של factor לתמיד" }
  ],
  "connections": [
    { "from": "call", "to": "made", "label": "factor = 2" },
    { "from": "made", "to": "use" }
  ],
  "animation": {
    "step_seconds": 1.7,
    "steps": [
      { "active": ["factory"] },
      { "active": ["call"] },
      { "active": ["made"], "edges": [0] },
      { "active": ["use"], "edges": [1], "set": { "use": 1 } },
      { "active": ["use"], "set": { "use": 2 } }
    ]
  }
}
