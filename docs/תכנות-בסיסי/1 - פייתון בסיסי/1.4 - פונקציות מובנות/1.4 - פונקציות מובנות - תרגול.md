
## הממוצע
- בהינתן רשימה של ציונים, למשל: `[76,89,54,98,43,47,83]` מצא את הציון הממוצע עם שימוש הפונקציות המובנות  `sum` ו - `len`.

## המקס
- בהינתן 3 מספרים כקלט החזיר את המספר הגדול ביותר עם שימוש ב `input` ו - `max`

## הפוך אותי
- כתוב קוד שמקבל שתי מספרים שלמים (int) ומדפיס את תוצאת החילוק שלהם, שימו לב שקיבלנו מספר עשרוני (float), השתמשו בtype casting כדי להדפיס את התוצאה כמספר שלם - לא עשרוני.