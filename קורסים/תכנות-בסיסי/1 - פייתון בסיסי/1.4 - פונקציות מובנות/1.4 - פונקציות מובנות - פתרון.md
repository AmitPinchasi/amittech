## הממוצע
```python
# Exercise: given a list of grades for an example `[76,89,54,98,43,47,83]` find the average using `sum` and `len` built-ins
numbers = [76,89,54,98,43,47,83]
average_number = sum(numbers) / len(numbers)
print(f"The average number is {average_number}")
```

## המקס
```python
# Exercise: given 3 numbers inputs find the maximum value using `input`, and `max`
first_number = input("Enter first_number: ")
second_number = input("Enter second_number: ")
third_number = input("Enter third_number: ")

numbers = [first_number, second_number, third_number]
maximum_number = max(numbers)
print(f"The maximum number is {maximum_number}")
```

## הפוך  אותי
```python
first_number = input("Enter first_number: ")
second_number = input("Enter second_number: ")

result = int(first_number/second_number)

print(f"result is {result}")
```